# Personal-Fitness-Tracker

## Setup
1. Create environment: `python -m venv my_custom_env_name`
2. Activate: `source my_custom_env_name/bin/activate`
3. Install deps: `pip install -r requirements.txt`

## Tests
python -m tests.crud